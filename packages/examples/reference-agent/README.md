# `@keepertoll/reference-agent`

Reference TypeScript agent. Imports the SDK and demonstrates the end-to-end
caller flow.

> **Status: Phase 1 stub.** Currently prints `agent ready`. Phase 4 turns this
> into a real demo against the reference workflow.

## Run

```bash
pnpm --filter @keepertoll/reference-agent dev
```
