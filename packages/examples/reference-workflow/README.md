# `@keepertoll/reference-workflow`

KeeperHub workflow JSON shown in the demo. Phase 4 deliverable: a workflow
named `MultiChainAaveHealth` that returns the health factor of an Aave v3
position across Ethereum, Base, and Arbitrum, priced at $0.02 per call.

> **Status: Phase 1 placeholder.** The workflow JSON drops here in Phase 4
> as `workflow.json`, alongside recreation steps for a reviewer to import it
> into their own KeeperHub org.

## Files (planned)

| File | Purpose |
|---|---|
| `workflow.json` | Exported KeeperHub workflow, suitable for `kh workflow import`. |
| `recreate.md` | Click-through recreation in case the JSON drifts from KeeperHub's schema. |
